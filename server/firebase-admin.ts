// For Firebase Admin SDK initialization
console.log('Firebase Admin SDK module loaded');

// We're exporting a simple object to avoid initialization errors
// This is a temporary development solution
const app = null;
export const adminDb = null;
export const adminAuth = null;
export default app;